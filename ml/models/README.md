# Model Artifacts

Store model metadata and label-map documentation here.

Large `.tflite` files should be handled by Git LFS, release assets, or a private artifact store after that decision is made.
